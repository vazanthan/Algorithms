Objective:
==========
	Default inheritance of a class in private.

Logic:
======
	When we derive a base class from a parent class, the default access specifier becomes private. So we cannot access the members of
	derived class just like that as it is private.
	But when we derive a 'struct' from a class/struct then the default acccess specifier is public. So we can access
