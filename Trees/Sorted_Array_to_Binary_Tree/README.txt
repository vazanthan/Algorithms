Objective:
==========
	Given an array, form a binary tree from it.

Logic:
======
	The logic is same as that of forming a binary tree from an array. The idea is that, take the 1st element from the queue. Add the next two elements as the left child and right child. Push those two elements in the queue. Do this till the array is empty.
